<Window x:Class="MeetingOrganiserDesktopApp.View.GuestListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MeetingOrganiserDesktopApp.View"
        mc:Ignorable="d"
        Title="GuestListWindow" Height="450" Width="800">
    <DataGrid Name="memberGrid" Grid.Row="0" Grid.Column="0"  AutoGenerateColumns="False" IsReadOnly="True" ItemsSource="{Binding GuestList}" SelectedItem="{Binding SelectedMember}">
        <DataGrid.Columns>
            <DataGridTextColumn Header="Name" Binding="{Binding Name}" />
            <DataGridTextColumn Header="Email" Binding="{Binding Email}"  />
            <DataGridTextColumn Header="Date of joining" Binding="{Binding DateOfJoining}"  />
            <DataGridTextColumn Header="Department" Binding="{Binding Department}" />
            <DataGridTextColumn Header="Role/jobtitle" Binding="{Binding Job}"  />
            <DataGridTextColumn Header="Supervisor" Binding="{Binding Boss.Name}" />
        </DataGrid.Columns>
    </DataGrid>
</Window>
