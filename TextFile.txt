<DataGrid Name="projectGrid" Grid.Row="2" Grid.Column="0"  AutoGenerateColumns="False" IsReadOnly="True" ItemsSource="{Binding SelectedMember.Projects}" SelectedItem="{Binding SelectedProject}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" />
                <DataGridTextColumn Header="Importance" Binding="{Binding Weight}"  />
            </DataGrid.Columns>
        </DataGrid>

<StackPanel Grid.Row="3" Grid.Column="0" Orientation="Horizontal" Grid.ColumnSpan="2" Visibility="Hidden">
            <Button Content="Add existing project to member" Margin="6,5,0,5" Command="{Binding AddProjectCommand}" Width="114" IsEnabled="{Binding IsLoaded}" />
            <Button Content="Add new" Margin="6,5,0,5" Command="{Binding CreateProjectCommand}" Width="114" IsEnabled="{Binding IsLoaded}" />
            <Button Content="Edit selected" Margin="6,5,0,5" Command="{Binding UpdateProjectCommand}" CommandParameter="{Binding SelectedEvent}" Width="114" IsEnabled="{Binding IsLoaded}" />
            <Button Content="Delete selected" Margin="6,5,6,5" Command="{Binding DeleteProjectCommand}" CommandParameter="{Binding SelectedEvent}" Width="114" IsEnabled="{Binding IsLoaded}" />
        </StackPanel>